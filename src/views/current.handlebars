<div>
    <h1>Tu perfil (sin datos sensibles)</h1>
    <p>{{user.first_name}}</p>
    <p>{{user.last_name}}</p>
    <p>{{user.email}}</p>
    <p>{{user.age}}</p>
    <p>{{user.githubProfile}}</p>
    <p>role: {{user.role}}</p>
    <butto class="logout">Logout</butto>
</div>
<script src="/js/prods.js"></script>